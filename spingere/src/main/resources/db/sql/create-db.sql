CREATE TABLE USERS (
    id              INTEGER     NOT NULL,
    name            VARCHAR(30) NOT NULL,
    email           VARCHAR(50) NOT NULL,
    modificado      TIMESTAMP   NOT NULL, 
    PRIMARY KEY(id)
);